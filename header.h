#ifndef HEADER_H
#define HEADER_H
#include <iostream>
#include <vector>
#include <map>
#include <stack>
#include <fstream>
#include <cmath>
#include <string.h>
using namespace std;
extern map<string, double> value;
extern map<string, int> idx;
extern vector<bool> bad;
extern vector<vector<string> > postfixTokens;

#endif